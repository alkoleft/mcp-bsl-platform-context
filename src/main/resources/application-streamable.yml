# Профиль для Streamable HTTP режима
spring:
  ai:
    mcp:
      server:
        stdio: false
        protocol: STREAMABLE
        streamable-http:
          mcp-endpoint: /mcp
server:
  port: ${MCP_HTTP_PORT:${SSE_PORT:8080}}
